# Default target language

Used only when `/think-en` is invoked with no language argument *and* the user's message is in
English. Change the code below to your own language.

    ru

Any ISO 639-1 code works. A matching `references/<code>.md` profile is optional — without one the
core rules still apply.
