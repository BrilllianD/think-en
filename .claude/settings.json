{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath // empty' | { read -r f; case \"$f\" in *.json) jq -e . \"$f\" >/dev/null 2>&1 || { echo \"Invalid JSON: $f\" >&2; exit 2; };; esac; }",
            "timeout": 10,
            "statusMessage": "Validating JSON..."
          }
        ]
      }
    ]
  }
}
